import SiteNavLayout from './SiteNavLayout'

export const generated = (args) => {
  return <SiteNavLayout {...args} />
}

export default {
  title: 'Layouts/SiteNavLayout',
  component: SiteNavLayout,
}
